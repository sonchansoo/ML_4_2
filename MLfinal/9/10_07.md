## 🔍 강의 요약

이번 강의에서는 머신러닝의 비지도 학습 방법 중 하나인 **군집화(Clustering)** 알고리즘에 대해 학습합니다. 특히, K-Means 알고리즘의 한계를 알아보고, 이를 보완하는 **Expectation-Maximization (EM) 알고리즘**과 **Gaussian Mixture Model (GMM)**에 대해 심도 있게 다룹니다. EM 알고리즘이 숨겨진 변수(latent variable)가 있는 상황에서 어떻게 모델의 파라미터를 추정하는지 동전 던지기 예시를 통해 직관적으로 이해하고, GMM이 데이터를 여러 개의 가우시안 분포의 혼합으로 간주하여 부드러운 군집화(soft clustering)를 수행하는 원리를 배웁니다.

## 💡 핵심 포인트

- **K-Means vs. EM Clustering:** K-Means는 원형(spherical) 군집을 가정하는 **Hard Clustering**인 반면, EM은 타원형(elliptical) 군집을 가정하는 **Soft Clustering**입니다.
- **Soft Clustering:** 각 데이터 포인트를 하나의 군집에만 할당하는 대신, 여러 군집에 속할 **확률**을 할당하는 방식입니다.
- **EM (Expectation-Maximization) 알고리즘:** 숨겨진 변수가 있을 때, 모델의 파라미터를 찾기 위한 반복적인 알고리즘입니다.
    - **E-step (Expectation):** 현재 파라미터를 기반으로 숨겨진 변수의 확률(기댓값)을 계산합니다.
    - **M-step (Maximization):** E-step에서 계산된 확률을 이용해 모델의 가능도(likelihood)를 최대화하는 새로운 파라미터를 추정합니다.
- **GMM (Gaussian Mixture Model):** 데이터가 K개의 가우시안(정규) 분포의 혼합으로 이루어져 있다고 가정하는 확률 모델이며, EM 알고리즘을 통해 각 분포의 파라미터를 학습합니다.

## 세부 내용 📚

### 1️⃣ K-Means의 한계와 EM Clustering의 등장

- **K-Means Clustering**은 각 군집의 중심으로부터의 거리를 기반으로 동작하기 때문에, 결과적으로 **원형(spherical) 형태의 군집**을 형성하는 경향이 있습니다.
- 이는 각 데이터 포인트를 단 하나의 군집에만 할당하는 **'Hard Clustering'** 방식입니다.
- 하지만 데이터 분포가 길쭉하거나 복잡한 형태일 경우, K-Means는 군집을 정확하게 찾아내기 어렵습니다.
- **EM Clustering**은 이러한 한계를 극복하기 위해 등장했습니다.
    - **타원형(elliptical) 군집**을 가정하여 더 유연한 형태의 군집을 찾을 수 있습니다.
    - 각 데이터가 특정 군집에 속할 **확률(probability)**을 계산하는 **'Soft Clustering'** 방식을 사용합니다.

### 2️⃣ EM (Expectation-Maximization) 알고리즘의 원리

- EM 알고리즘은 데이터에 **숨겨진 변수(latent variable)**가 존재할 때, **최대 가능도 추정(Maximum Likelihood Estimation, MLE)**을 통해 최적의 모델 파라미터를 찾는 방법입니다.
- 알고리즘은 두 단계를 반복적으로 수행합니다.
    - **E-step (기댓값 단계):** 현재 추정된 모델 파라미터를 사용하여, 각 데이터 포인트에 대한 숨겨진 변수의 확률(기댓값)을 계산합니다. (예: 이 데이터가 1번 군집에서 왔을 확률은 80%, 2번 군집에서 왔을 확률은 20%라고 계산)
    - **M-step (최대화 단계):** E-step에서 계산된 확률을 가중치처럼 사용하여, 모델의 가능도를 최대화하는 새로운 파라미터(예: 군집의 평균, 공분산)를 추정합니다.
- 이 과정을 파라미터 값이 더 이상 변하지 않고 **수렴(converge)**할 때까지 반복합니다.

### 3️⃣ 동전 던지기 예시로 본 EM 알고리즘 🪙

- **문제 상황:** 두 개의 다른 동전 A, B가 있지만, 각 던지기 결과가 어떤 동전에서 나왔는지 모르는 **(숨겨진 변수)** 상태에서, 각 동전(A, B)의 앞면이 나올 확률(p, q)을 추정해야 합니다.
- **EM 알고리즘 적용:**
    1.  **초기화:** p와 q의 값을 임의로 추측합니다 (예: p=0.6, q=0.5).
    2.  **E-step:** 현재의 p, q 값을 바탕으로, 관찰된 각 동전 던지기 결과(예: '앞면 5번, 뒷면 5번')가 동전 A에서 나왔을 확률과 동전 B에서 나왔을 확률을 각각 계산합니다.
    3.  **M-step:** E-step에서 계산된 확률을 이용해, 동전 A와 B 각각에 대해 기대되는 앞면과 뒷면의 횟수를 계산하고, 이를 바탕으로 p와 q 값을 새로 업데이트합니다.
    4.  **반복:** 새로운 p, q 값으로 E-step과 M-step을 다시 수행하며, p와 q 값이 수렴할 때까지 반복합니다.

### 4️⃣ GMM (Gaussian Mixture Model)과 EM의 관계

- **GMM**은 전체 데이터가 **K개의 가우시안(정규) 분포가 섞여서** 만들어졌다고 가정하는 모델입니다.
- 즉, K개의 군집이 존재하며, 각 군집은 고유한 평균(μ)과 공분산(Σ)을 가진 가우시안 분포를 따릅니다.
- 여기서 **숨겨진 변수**는 '각 데이터 포인트가 K개의 가우시안 분포 중 어느 분포에서 생성되었는가'입니다.
- **EM 알고리즘**은 GMM에서 이 숨겨진 변수를 추정하고, 각 가우시안 분포의 최적 파라미터(가중치, 평균, 공분산)를 찾아내는 핵심적인 역할을 수행합니다.

## 오늘의 연습문제 📝

- **문제 1:** K-Means Clustering과 EM Clustering (GMM)의 가장 큰 차이점 두 가지를 설명하고, 어떤 데이터 분포에서 EM Clustering이 더 효과적일지 설명해 보세요.
- **문제 2:** EM 알고리즘의 'E-step'과 'M-step'이 각각 무엇을 의미하며, 동전 던지기 예시에서 각 단계가 어떻게 적용되는지 간략히 설명해 보세요.
- **문제 3:** '숨겨진 변수(latent variable)'란 무엇이며, Gaussian Mixture Model에서 숨겨진 변수는 구체적으로 무엇에 해당할까요?
