## 🔍 강의 요약
이번 강의에서는 대표적인 밀도 기반 클러스터링 알고리즘인 **DBSCAN**에 대해 학습합니다. 기존의 K-means나 EM-clustering이 처리하기 어려운 비정형 데이터(non-Gaussian data)를 효과적으로 군집화하는 방법을 알아봅니다. DBSCAN의 핵심 개념인 `Eps`와 `MinPts` 파라미터, 그리고 이를 통해 정의되는 Core, Border, Noise Point에 대해 이해하고, 알고리즘의 동작 원리와 장단점을 파악합니다. 마지막으로 Scikit-Learn을 활용한 실제 구현 과정도 간략히 살펴봅니다.

## 💡 핵심 포인트
- **밀도 기반 클러스터링**: 데이터의 밀도를 기반으로 군집을 형성하는 방식입니다.
- **DBSCAN**: 임의의 형태를 가진 클러스터를 발견하고, 노이즈(Noise)를 효과적으로 처리할 수 있는 알고리즘입니다.
- **주요 파라미터**: `Eps(ε)` (이웃 반경)와 `MinPts` (최소 데이터 수)라는 두 가지 핵심 파라미터를 사용합니다.
- **포인트 유형**: 데이터를 Core Point, Border Point, Noise Point 세 가지로 분류하여 클러스터를 형성합니다.
- **장단점**: 노이즈에 강하고 다양한 형태의 군집을 찾을 수 있지만, 데이터 밀도가 균일하지 않거나 파라미터 설정에 민감한 단점이 있습니다.

## 세부 내용 📚
### 1️⃣ 기존 클러스터링의 한계와 밀도 기반 클러스터링
- **K-means, EM-clustering의 한계**: 이 알고리즘들은 데이터가 가우시안 분포(Gaussian distribution)를 따른다고 가정하므로, 원형이 아닌 복잡한 형태의 클러스터(예: 두 개의 연결된 원)를 잘 구분하지 못합니다.
- **밀도 기반 클러스터링의 등장**: 이 문제를 해결하기 위해 등장했으며, 데이터가 밀집된 영역을 하나의 클러스터로 간주합니다. 클러스터는 밀도가 높은 지역이며, 밀도가 낮은 지역에 의해 다른 클러스터와 분리됩니다.
- **주요 알고리즘**: DBSCAN, DENCLUE, OPTICS, CLIQUE 등이 있습니다.

### 2️⃣ DBSCAN의 핵심 개념과 파라미터
- **DBSCAN**: **D**ensity-**B**ased **S**patial **C**lustering of **A**pplications with **N**oise의 약자입니다.
- **두 가지 핵심 파라미터**:
    - **`Eps (ε)`**: 하나의 데이터 포인트를 기준으로 이웃을 정의하는 반경 거리입니다.
    - **`MinPts`**: 하나의 클러스터로 인정받기 위해 `Eps` 반경 내에 존재해야 하는 최소 데이터 포인트의 수입니다.
- **세 가지 포인트 유형**:
    - **Core Point (핵심 포인트)**: `Eps` 반경 내에 `MinPts` 개 이상의 포인트(자기 자신 포함)를 가진 포인트입니다. 클러스터의 내부에 위치합니다.
    - **Border Point (경계 포인트)**: `Eps` 반경 내 포인트 수가 `MinPts`보다 적지만, Core Point의 이웃에 속하는 포인트입니다. 클러스터의 외곽을 형성합니다.
    - **Noise Point (노이즈 포인트)**: Core Point도, Border Point도 아닌 포인트로, 어떤 클러스터에도 속하지 않는 이상치(outlier)입니다.

### 3️⃣ DBSCAN 알고리즘의 동작 원리 및 복잡도
- **클러스터 형성 과정**:
    1. 임의의 포인트를 선택합니다.
    2. 해당 포인트가 Core Point인지 확인합니다.
    3. Core Point이면 새로운 클러스터를 생성하고, 이웃한 모든 포인트를 클러스터에 추가(확장)합니다.
    4. Core Point가 아니면 Noise로 잠정 분류합니다.
    5. 모든 포인트에 대해 위 과정을 반복합니다.
- **시간 복잡도**: 모든 포인트에 대해 이웃을 찾아야 하므로 일반적으로 **O(n²)**입니다. 효율적인 자료구조를 사용하면 **O(n*log(n))**까지 줄일 수 있습니다.
- **공간 복잡도**: **O(n)**

### 4️⃣ DBSCAN의 장단점 및 파라미터 설정 팁
- **장점**:
    - 노이즈에 강건하고 자동으로 이상치를 분류합니다.
    - K-means와 달리 클러스터 개수를 미리 지정할 필요가 없습니다.
    - 다양한 크기와 형태의 클러스터를 발견할 수 있습니다.
- **단점**:
    - 데이터 밀도가 다양한 경우 잘 동작하지 않습니다. (예: 한쪽은 빽빽하고 다른 쪽은 듬성듬성한 데이터)
    - 고차원 데이터(high dimensions)에서는 성능이 저하될 수 있습니다.
    - `Eps`와 `MinPts` 파라미터 설정에 매우 민감하여, 최적의 값을 찾기 어려울 수 있습니다.
- **파라미터 설정 팁**:
    - `k-distance plot`을 사용하여 적절한 `Eps` 값을 추정할 수 있습니다. (k = `MinPts`) 그래프가 급격히 꺾이는 'elbow' 지점을 `Eps` 값으로 선택하는 것이 일반적입니다.

## 오늘의 연습문제 📝
- **문제 1**: DBSCAN 알고리즘에서 `Eps`와 `MinPts` 파라미터가 각각 어떤 역할을 하는지, 그리고 이 두 파라미터가 '밀도가 높은 지역'을 어떻게 정의하는지 설명해 보세요.
- **문제 2**: Core Point, Border Point, Noise Point의 차이점을 설명하고, Border Point가 클러스터에 포함될 수 있는 이유를 설명해 보세요.
- **문제 3**: DBSCAN 알고리즘이 좋은 성능을 내기 어려운 데이터셋의 예시를 한 가지 들고, 그 이유를 알고리즘의 단점과 연관 지어 설명해 보세요.
